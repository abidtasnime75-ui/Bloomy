<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ✅ PERMISSIONS INTERNET (OBLIGATOIRES POUR FIREBASE) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:label="🌿 Bloomy"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">

        <!-- ✅ ACTIVITÉ PRINCIPALE -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Thème Flutter -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- Intent Filter pour lancer l'app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ✅ Méta-données Flutter (NE PAS SUPPRIMER) -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ✅ Méta-données Firebase (automatique avec google-services) -->
        <!-- Le plugin google-services ajoute automatiquement les configs nécessaires -->

    </application>

    <!-- ✅ Requêtes pour le traitement de texte (Flutter) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>